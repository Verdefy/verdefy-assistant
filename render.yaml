services:
  - type: web
    name: verdefy-groq-assistant
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: GROQ_API_KEY
        value: YOUR_API_KEY # <-- don't write real key here again!
